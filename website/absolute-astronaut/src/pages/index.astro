---
import { siteUrl } from "../utils/siteUrl";

const acceptLang = Astro.request.headers.get("accept-language") || "";
const prefersId = acceptLang.toLowerCase().startsWith("id");
return Astro.redirect(siteUrl(prefersId ? "id/" : "en/"));
---
